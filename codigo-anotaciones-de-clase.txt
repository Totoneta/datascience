.ipynb
# IMPORTACIONES
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

########## TEORIA ########## 
Cuando la distribcion es uniforme: variable con misma cant de poroblabilidad(Barras de misma altura en una variable)

Cuando la distribucion es normal: campana de gauss
zscore = dato de variable - la media / desvio 
	-- Cuantos desvios encuentra respecto a ese dato de variable
	-- Si es 0 esta centrado en el promedio
	-- En 3 desvios tenemos 99,73% de probabilidad
	-- Valor mayor que 3 te da demaciada dispercion en los outliners


########## PANDAS ########## 

df.head(n°) - Muestra los primeros ejemplos
df.tails(n°) - Muestra los últimos ejemplos
df.sample(n°, random_state=n°) - Muestra los ejemplos aleatorios(random asegura que siempre sea la misma muestra de datos)
df.shape - filas, col
df.info() - nulls count and df lit descr
df.isna().sum() - suma valoresnull de las cols
df.columna.describe() - saca la media, la mediana, moda, percentiles, minimo, maximo y desvio
df.algo.T - Invierte filas y cols
df.select_dtypes(include=[np.number]) - Retorna las cols con tipo numero, y por usar nunpy, no hace falta aclarar si float,int,etc

df.columna.unique() - Que opciones se podrian colocar en esa col 

df.columns - muestra las columnas
df.drop(columns=["nombrecol"]) - Eliminar columna
df.columna.value_counts() - retorna cantidad de cada opcion

df = df.read_csv("Ruta C:/... .csv", usecols=[""nombrecol"], header=None) - Lee un archivo CSV (usecols seutiliza para seleccionar las columnas que queres leer, header lee los encabezados o los remplaza por un índice)
df.to_csv("ruta C:/... .csv") - guardar el archivo procesado
df = df.read_json("Ruta C:/... .json") - Lee un archivo JSON
df = df.read_excel("Ruta C:/... .xlxs") - Lee un archivo EXCEL

df.corr() -- Retorna la matrix

.reset_index(drop=True, inplace=True) - Resetea el index al tamaño de muestra que se selecciono (drop pierde la columna indice anterior e inplace la recoloca ordenada)

########## MATPLOTLIB ########## 

plt.figure(figsize=(10,6)) - tamaño y creacion de lienzo
plt.show() - Muestra el gráfico
plt.title("... ") - Título de gráfico
plt.ylabel("...") -- cambias las etiquetas y
plt.xlabel("...") -- cambias las etiquetas x

/////////////////////////// Gráficos /////////////////////////// 
* pasar datos en "dato" -- ej:df.value_count() 

plt.pie(dato, labels=dato.index) - Grafico de torta con titulo basado en el index de dato

########## SEABORN ########## 

sns.countplot(
data=df, 
x="nombrecolumna", 
hue="otracolumna") -- Grafico de barras por columna (hue, comparativa con otra col)
#Comparativa de x=cualitativo e y=cuantitativo, entre 2 o mas variables

sns.histplot(
data=df,
x="nombrecolumna", 
bins=n°, 
kde=True) -- Grafica historigrama (bins, cantidad de barras/kde, linea promedio)
#Compara una variable a lo largo del tiempo

sns.boxplot(
data=df) -- Grafica en cajas y vigotes
#Comparativa de x=cualitativa e y=cuantitativa/nada, de 1 variable y sus outliners

sns.scatterplot(
data=df,  
x="nombrecolumna", 
y="nombrecolumna") -- Grafico de puntos
#Comparativa x=cuantitativa e y=cuantitativa, entre dos variables

sns.heatmap(
data)
#Muestra las relaciones entre variables























































































































